<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Force HTTPS
    |--------------------------------------------------------------------------
    |
    | This option forces the application to use HTTPS. This is useful when
    | running behind a load balancer or reverse proxy that terminates SSL.
    |
    */
    'force_https' => env('FORCE_HTTPS', false),
];
